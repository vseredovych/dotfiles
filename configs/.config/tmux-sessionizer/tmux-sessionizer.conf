TS_SEARCH_PATHS=("$HOME/repos" "$HOME/dev")
TS_MAX_DEPTH=1

# Ad-hoc session slots — fill these in per machine.
# Used by: tmux-sessionizer -s 0  (mapped to Alt+H in nvim)
#           tmux-sessionizer -s 1  (mapped to Alt+T in nvim)
#           tmux-sessionizer -s 2  (mapped to Alt+N in nvim)
#           tmux-sessionizer -s 3  (mapped to Alt+S in nvim)
# Examples: a project path, a shell command, a session name, etc.
TS_SESSION_COMMANDS=(
    ""  # slot 0 (Alt+H)
    ""  # slot 1 (Alt+T)
    ""  # slot 2 (Alt+N)
    ""  # slot 3 (Alt+S)
)
